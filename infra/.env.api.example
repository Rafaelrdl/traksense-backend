# ============================================================================
# Variáveis de Ambiente - Django API (Desenvolvimento) - TEMPLATE
# ============================================================================
# Este é um arquivo EXEMPLO para configuração do Django API.
# 
# INSTRUÇÕES:
# 1. Copie este arquivo: cp .env.api.example .env.api
# 2. Ajuste os valores conforme seu ambiente
# 3. NUNCA commite .env.api no Git (já está no .gitignore)
#
# Para produção, gerar valores seguros:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# ============================================================================

# Django Debug Mode (1=True, 0=False)
# CRÍTICO: Sempre 0 em produção!
DJANGO_DEBUG=1

# Chave secreta Django (gerar valor aleatório em produção!)
DJANGO_SECRET_KEY=dev-secret-key-change-me

# String de conexão PostgreSQL/TimescaleDB
DATABASE_URL=postgresql://postgres:postgres@db:5432/traksense

# String de conexão Redis (cache e Celery)
REDIS_URL=redis://redis:6379/0

# Hosts permitidos (usar domínios específicos em produção)
ALLOWED_HOSTS=*

# ============================================================================
# PRODUÇÃO: Descomentar e configurar
# ============================================================================
# CORS_ALLOWED_ORIGINS=https://app.traksense.com
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=SG.xxx
# SENTRY_DSN=https://xxx@sentry.io/xxx
# ============================================================================
